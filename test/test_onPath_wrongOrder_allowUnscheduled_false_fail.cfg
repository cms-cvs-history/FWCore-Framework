process TEST = {
  	source = EmptySource {
          untracked int32 maxEvents = 3
	  untracked uint32 firstTime = 1000000
	  untracked uint32 timeBetweenEvents = 10
	}

	untracked PSet options = {
		untracked bool allowUnscheduled = false
		untracked vstring Rethrow = {"ProductNotFound","ScheduleExecutionFailure", "ModuleFailure"}
	}

	service = Tracer {}

	module one = IntProducer {
	  int32 ivalue = 1
	}

	module two = IntProducer {
	  int32 ivalue = 2
	}

	module getOne = IntTestAnalyzer {
	  untracked int32 valueMustMatch = 1
	  untracked string moduleLabel = "one"
	}

	module getTwo = IntTestAnalyzer {
	  untracked int32 valueMustMatch = 2
	  untracked string moduleLabel = "two"
	}

	path p = {two,getOne & one,getTwo}
}
