process TEST = {
  	source = EmptySource {
          untracked int32 maxEvents = 3
	  untracked uint32 firstTime = 1000000
	  untracked uint32 timeBetweenEvents = 10
	}

	untracked PSet options = {
		untracked bool allowUnscheduled = true
		untracked vstring Rethrow = {"ProductNotFound","ScheduleExecutionFailure", "ModuleFailure"}
	}

	service = Tracer {}

	module one = IntProducer {
	  int32 ivalue = 1
	}

	module result1 = AddIntsProducer {
	  vstring labels = {"one"}
	}

	module result2 = AddIntsProducer {
	  vstring labels = {"result1","one"}
	}

	module result4 = AddIntsProducer {
	  vstring labels = {"result2", "result2"} 
	}

	module get = IntTestAnalyzer {
	  untracked int32 valueMustMatch = 4
	  untracked string moduleLabel = "result4"
	}


	path p = {get}
}
