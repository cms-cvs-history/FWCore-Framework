
process TEST  = {
        include "FWCore/Framework/test/cmsExceptionsFatal.cff"
	source = EmptySource { untracked int32 maxEvents = 5 }
	module m = TestESDummyDataAnalyzer { 
          int32 expected = 5
          untracked int32 nEvents = 10 
        }
	looper = DummyLooper { untracked int32 value = 4 }
        
        es_module = LoadableDummyProvider { untracked int32 value = 5 }

        es_prefer = LoadableDummyProvider {}

        path p1 = { m }
}
